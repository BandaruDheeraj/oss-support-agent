# OI-001 - Represent deterministic non-LLM stages with first-class OpenInference semantics

## 1) Snapshot

| Field | Value |
|-------|-------|
| Gap ID | OI-001 |
| Owner | openinference |
| Status | new |
| Priority | P1 |
| Job | Represent deterministic non-LLM stages with first-class OpenInference semantics |
| Competitor platform | langsmith |
| Last run artifact | evals/gap-runs/2026-06-01T00-44-12-411Z.json |
| Last updated | 2026-06-01T00:44:12.411Z |

## 2) What competitors do (why easier)

- **Competitor mechanism:** LangSmith run model captures stage runs with explicit run type and structured input/output payloads.
- **Why easier:** Deterministic stages can remain structured without inventing custom attributes.

## 3) What we do today in oss-support-agent

- **Current behavior:** Adapter maps `phase` and fallback kinds to `CHAIN`, then stores deterministic outputs as JSON blobs.
- **Affected components:** core/observability/arize.ts, evals/run-eval.ts

## 4) Gap statement

Because Adapter maps `phase` and fallback kinds to `CHAIN`, then stores deterministic outputs as JSON blobs., users cannot Represent deterministic non-LLM stages with first-class OpenInference semantics as easily as with LangSmith run model captures stage runs with explicit run type and structured input/output payloads..

## 5) Proposed fix

- **Owner-scoped change:** Extend OpenInference conventions with deterministic-stage guidance and reference examples.
- **Root cause:** OpenInference span-kind taxonomy lacks a dedicated deterministic stage concept.

## 6) Delivery plan

1. Validate via `npm run trace-smoke`
2. Validate via `npm run eval:observability`

### Dependencies and risks

- **Dependencies:** Requires openinference SDK/spec changes
- **Risks:** May require coordination with OTel upstream for OpenInference gaps

## 7) Success metrics

| Metric | Baseline | Target | How measured |
|--------|----------|--------|--------------|
| Deterministic stage traces are queryable without adapter-specific custom keys. | current | improved | npm run trace-smoke |

## 8) Evidence and citations

- `core/observability/arize.ts:56-68` — Current kind mapping collapses phase/default to `CHAIN`.
- `evals/run-eval.ts:507` — Cross-platform note on deterministic-stage awkwardness.
- `evals/SETUP-FRICTION.md:33` — Friction statement on missing clean deterministic span kind.

## 9) Change log

- 2026-06-01T00:44:12.411Z — created from evals/gap-runs/2026-06-01T00-44-12-411Z.json
